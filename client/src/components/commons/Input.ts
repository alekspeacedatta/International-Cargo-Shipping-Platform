import styled from "styled-components";

export const Input = styled.input`
        width: 14vw;
        padding: 8px 18px;
        border-radius: 3px;
        border: 2px solid #dadddf;
        outline: none;
        transition: all .2s;
`;